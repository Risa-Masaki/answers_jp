<div class="contents row">
  <%= form_for @question do |f| %>
    <h3>
      EDIT YOUR QUESTION
    </h3>
        <div class="form-group">
          <div class="col-lg-6">
            <%= f.text_field :text, placeholder: "Answer Friend's Question", class: "form-control" %>
          </div>
        </div>
        <div class="form-group">
          <%= f.submit "SEND", class: "btn btn-primary withripple" %>
        </div>
      <% end %>
<!--   <%= form_tag("/questions/#{@question.id}", method: :patch ) do %>
    <h3>
      EDIT YOUR QUESTION
    </h3>
    <input placeholder="Image Url" type="text" name="image" value="<%= @question.image %>" autofocus="true">
    <textarea cols="30" name="text" placeholder="text" rows="10"><%= @question.text %></textarea>
    <input type="submit" value="SENT">
  <% end %> -->
</div>
